apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ingress.yaml

replicas:
- name: test-rbac-manager
  count: 5

configMapGenerator:
- name: squid.conf
  files:
  - squid.conf
